/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'images.pexels.com', 'webmobtechcdn.nyc3.cdn.digitaloceanspaces.com'],
  },
};

export default nextConfig;
